	我們這組的程式碼有使用改變字體顏色，目前測試是只能使用vs code和colab可以正常運作，
其餘均會直接把代碼印出來。但是我們還有include windows.h的標頭檔，因此也無法在colab使用，都
是用vs code測試。
	我們還有做一個額外的額外功能，就是另外顯示計時時間。原本程式碼就有計時的功能了，
在開始和結束的時候分別開啟然後關閉執行檔，當作給使用者的參考(vs code分割畫面在左邊，
timer console會自動在右邊開啟)。原始碼都附在壓縮檔裡面了。
